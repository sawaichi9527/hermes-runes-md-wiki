# Hermes Runes MD Wiki - default fresh-clone requirements
#
# Default install must stay lightweight for personal-local users.
# Do not add torch, sentence-transformers, CUDA, Triton, or full pip freeze here.
#
# For optional hybrid/vector embedding support, use:
#   bash ./bin/hermes-memory-bootstrap --with-embedding
# or install requirements-embedding.txt after installing CPU-only torch.

-r requirements-core.txt
