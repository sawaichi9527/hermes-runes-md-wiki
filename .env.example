# Moved

The runtime environment template has moved to:

```text
tools/importer/.env.example
```

Use:

```bash
cp tools/importer/.env.example tools/importer/.env
vi tools/importer/.env
```

The normal quickstart flow does not require a root `.env` file.
