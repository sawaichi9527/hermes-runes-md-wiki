#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT="${HERMES_RUNES_ROOT:-${HERMES_MEMORY_ROOT:-$(cd "$SCRIPT_DIR/.." && pwd)}}"

exec python3 "$ROOT/tools/runes/runes.py" "$@"
