#!/usr/bin/env bash
set -euo pipefail

PYTHON="${PYTHON:-python3}"
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
ROOT="${HERMES_RUNES_ROOT:-$(cd "$SCRIPT_DIR/.." && pwd)}"

exec "$PYTHON" "$ROOT/tools/wiki_migration_guard/migration_guard.py" "$@"
