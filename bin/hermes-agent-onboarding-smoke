#!/usr/bin/env bash
set -euo pipefail

ROOT="${HERMES_RUNES_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
exec python3 "$ROOT/tools/runes_shield/smoke_agent_onboarding_readonly.py" --root "$ROOT" --json "$@"
