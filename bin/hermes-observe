#!/usr/bin/env bash
set -euo pipefail

PYTHON="${PYTHON:-python3}"
ROOT="${HERMES_MEMORY_ROOT:-$HOME/workspace/hermes-memory}"
SCRIPT="$ROOT/tools/importer/hermes_observe.py"

exec "$PYTHON" "$SCRIPT" "$@"
