# Hermes Runes MD Wiki - development dependencies
-r requirements.txt
-r requirements-embedding.txt

# Currently no extra dev-only dependencies are required.
# Add pytest/ruff/mypy later only when used by the repo.
