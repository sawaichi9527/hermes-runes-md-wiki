# Hermes Runes MD Wiki - developer-only lightweight requirements
#
# Keep this profile lightweight by default.
# Do not automatically include requirements-embedding.txt here, because that may
# pull torch / sentence-transformers and make routine developer setup heavy.
#
# Developers who need hybrid/vector/full-smoke behavior should run:
#   bash ./bin/hermes-memory-bootstrap --with-embedding

-r requirements-core.txt

# Add pytest/ruff/mypy later only when they are actually used by the repo.
