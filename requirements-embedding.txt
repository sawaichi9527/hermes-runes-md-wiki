# Hermes Runes MD Wiki optional embedding/full-smoke requirements
#
# This profile is optional. It enables hybrid/vector/full smoke behavior.
#
# Recommended install path:
#
#   bash ./bin/hermes-memory-bootstrap --with-embedding
#
# The bootstrap script installs CPU-only torch first, then installs these
# dependencies, to avoid pulling large CUDA wheels during personal-local
# fresh clone setup.

sentence-transformers>=3,<6
