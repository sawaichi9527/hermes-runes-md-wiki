# Hermes Runes MD Wiki v0.7.4

Status: final release / tag pending  
Release date: 2026-06-27

## Summary

v0.7.4 is a documentation and governance-boundary release for optional PLUR runtime memory bridge reintegration.

This release does not add a new runtime helper, PLUR smoke suite, daemon, queue, telemetry path, enterprise workflow, or Hermes Agent core customization. It preserves the existing single-agent / agent-agnostic baseline and keeps Core FTS smoke as the lightweight required verification path.

## Highlights

- Reframed v0.7.4 around optional PLUR runtime memory bridge reintegration.
- Recorded S1-S6 scope:
  - detachable PLUR integration
  - minimal Hermes Agent native customization
  - PLUR runtime memory role and source priority
  - engram / episode / checkpoint / candidate policy
  - human-in-the-loop forge candidate flow
  - minimal PLUR memory hygiene and deployed-memory caution
- Recorded S7-S9 as design-only:
  - PLUR read-only discovery / status check design
  - runtime memory provider abstraction / Noop provider design
  - PLUR memory schema mapping design
- Paused S10 because PLUR read-only context summary value is unclear.
- Recorded S11 candidate dry-run flow as proposal-only.
- Recorded S12 smoke / verification / docs sync as manual consistency checking.
- Removed the premature S7-S9 runtime helper and PLUR bridge smoke before release.

## Runtime boundary

v0.7.4 keeps the runtime surface unchanged:

```text
no new PLUR helper
no new PLUR smoke
no Hermes Agent core patch
no Hermes Agent native configuration change
no PLUR memory read
no PLUR memory write
no PLUR memory migration
no PLUR memory deletion
no automatic PLUR-to-Runes Wiki promotion
```

## PLUR bridge decision

The v0.7.4 PLUR bridge line is intentionally conservative:

```text
PLUR = optional runtime persistent working memory
Runes Wiki = governed canonical long-term memory source
Runes Shield = protected forge gate / operation protection layer
Hermes Agent / Lark bot = candidate proposer and task reasoner
User = human-in-the-loop approval authority
```

PLUR may support future runtime continuity, but it must not become the canonical memory authority or bypass explicit user approval and protected forge behavior.

## Verification baseline

Final local verification used:

```bash
cd ~/workspace/hermes-runes-md-wiki
git pull
git status
./bin/runes-wiki-migration-guard plan --no-fetch
./bin/hermes-memory-smoke
```

Observed result:

```text
VERSION = 0.7.4-dev at final design-only verification time
repo head = e84972c
working tree clean
migration guard SAFE
Core FTS smoke PASS
PLUR helper not added
PLUR smoke not added
Embedding profile skipped when not installed
```

## Release version

Final release version:

```text
0.7.4
```

## Release tag

Final release tag:

```text
v0.7.4
```

Tag commit:

```text
Pending: create tag after release commit is pulled locally.
```
